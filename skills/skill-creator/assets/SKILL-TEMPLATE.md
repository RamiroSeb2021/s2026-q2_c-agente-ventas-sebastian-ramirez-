---
name: {skill-name}
description: "Trigger: {essential trigger words}. {What this skill does}."
license: Apache-2.0
metadata:
  author: gentleman-programming
  version: "1.0"
---

## Activation Contract

Load this skill when {specific reusable situations}.

## Hard Rules

- {Non-negotiable constraint 1}.
- {Non-negotiable constraint 2}.
- Keep `SKILL.md` compact; move long details to `references/`.
- Put templates, schemas, fixtures, or generated examples in `assets/`.

## Decision Gates

| Situation | Action |
|---|---|
| {condition} | {action} |
| {condition} | {action} |

## Execution Steps

1. {First operational step}.
2. {Second operational step}.
3. {Verification or handoff step}.

## Output Contract

Return {required final format, files, or artifacts}.

## References

- `references/{details}.md` — {when to load supporting detail}.
- `assets/{template}.md` — {template or schema purpose}.
